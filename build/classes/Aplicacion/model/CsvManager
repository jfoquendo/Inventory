package Aplicacion.model;

import java.io.*;

public class CsvManager {
    public static void main(String[] args) {
        String archivo = "inventario.csv";

        try (BufferedReader reader = new BufferedReader(new FileReader(archivo))) {
            String linea;
            boolean primeraLinea = true; // Para omitir encabezados

            while ((linea = reader.readLine()) != null) {
                if (primeraLinea) { 
                    primeraLinea = false; // Saltar la primera línea (encabezados)
                    continue;
                }

                String[] datos = linea.split(";"); // Separar por comas
                String state = datos[0];
                String serial = datos[1];
                String model = datos[2];
                String  processor = datos[3];
                String ram = datos[4];
                String disk = datos[5];
                String username = datos[6];
                String id = datos[7];
                String userposition = datos[8];
                String costcenter = datos[9];
                String area = datos[10];
                String management = datos[11];
                String location = datos[12];
                String contract = datos[13];
                
                System.out.println(state+" "+ serial+" "+ model+" "+ processor+" "+ ram+" "+
                        disk+" "+ username+" "+ id+" "+ userposition+" "+costcenter+" "+area+" "+
                        management+" "+location+" "+contract);
            }
        } catch (IOException e) {
            System.out.println("Error al leer el archivo: " + e.getMessage());
        }
    }
}
